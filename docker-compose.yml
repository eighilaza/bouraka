db:
  image: postgres
  environment:
    - POSTGRES_PASSWORD=postgres
    - POSTGRES_DB=postgres
    - POSTGRES_USER=postgres
  volumes:
    - /root/bouraka-db:/var/lib/postgresql/data
web:
  image: local/bouraka:dev
  ports:
    - "12345:80"
  links:
    - db
